# 设计简报模板（填空后粘进 Claude Design）

> 用 Phase 0 发现的《项目设计上下文》填空。务必覆盖 `claude-design-context-spec.md` §2 的全部 context 维度。删除所有〈尖括号提示〉。

```markdown
# 〈模块名〉重设计简报（用于 Claude Design）
> 用法：claude.ai/design 新建 → 上传 current-page.html 作起点 → 整段粘贴本文 → 导出 standalone HTML 存回本文件夹。

## 0. 美学方向定调（先读，避免跑偏）
这是一个〈一句话定性：精炼数据终端 / 编辑型内容页 / …〉。它的"高级感"来自〈信息层级/数字主角/状态可辨/零噪音 …〉，而非装饰。
> ⚠️ 给 Claude Design：**压制你"氛围感/视觉冲击"的默认本能**（详见 context-spec §5）。数据看板严禁渐变网格/噪点纹理/戏剧化阴影/玻璃拟态/不对称重叠布局/彩虹色。唯一保留的本能=克制自信的排版 + 一种主色 + 锐利语义强调 + 发丝分隔。〈非看板类按需调整本段〉

## 1. 全局设计法则（取舍标尺，数据看板直接套 context-spec §6 七法则）
〈数据看板：粘贴/精简 context-spec §6——30秒健康判断 / 每数字带参照系 / 状态预注意编码 / BAN巨数字 / Tufte数据墨水 / 7±2分块 / 8px栅格节奏。非看板类换成对应领域法则。〉

## 一句话目标
为〈目标用户画像〉重做"〈模块名〉"。这是〈产品类型，如：数据密集型经营分析工具，非消费级 SaaS〉。核心诉求：〈一屏要看懂什么 + 要能定位到谁/哪个维度〉。保留全部数据维度与交互前提下，提升信息层级、可读性与品质。

## 目标受众与核心用例
- 受众：〈谁在用、在什么场景、什么设备〉
- 核心用例：1)〈…〉 2)〈…〉 3)〈…〉

## 当前状态与痛点
- 现状：见所上传 current-page.html（忠实还原当前页）。
- 痛点：〈现在哪里难用/难读/难看，列 2-4 条〉

## 必须保留的功能区块与交互（红线级）
〈逐条列 Phase 0 交互清单：页头/筛选/时间视图/双栏联动/Tab/下钻层级/可排序列/分页…每条标清交互方式〉

## 逐维度设计方向
- 排版：〈字体倾向；要避免的通用字体如 Arial/Inter；字号层级〉
- 配色：〈主色 + 点缀色；语义色映射；承诺用 CSS 变量保持一致；避免怯懦大杂色〉
- 动效：〈克制范围，如仅 hover/过渡；禁弹跳闪烁〉
- 背景/质感：〈底色与层级表达方式〉

## 设计系统约束（产出要能落地，来自本项目）
- 语义色：〈primary/success/warning/danger/neutral 实际色值〉——只在传递语义时上色。
- 字体栈：〈正文/数字/标题〉；数字等宽 tabular-nums。
- 组件预设：〈卡片/按钮/表格的视觉规格〉。
- 数值格式：〈率值小数位与 % 位置；金额单位与千分位；件数；空值符号〉。
- 暗色模式：〈是否需要 + 策略〉。
- 默认排序：〈如主题指标从差到好〉。

## 后端字段契约速查表（数据驱动页强制 — 防"假设字段总是有值"踩坑）
> 每个要展示的字段必须列出：单位、是否可能 NULL、哪个 SQL 文件/接口返回。AI 落地阶段会**假设**字段非空、单位匹配——契约缺失=必踩坑。

| 字段 | 单位 | 可能 NULL? | 真实数据源 | 注意 |
|------|------|-----------|----------|------|
| 〈premium〉 | 〈万元 / 元〉 | 〈是/否〉 | 〈server/src/sql/xxx.ts: 行号〉 | 〈如"summary.rows 中此字段写死 NULL，必须从 drilldown.summary 取"〉 |
| 〈achievement_rate〉 | 〈百分比 / 0-1 小数〉 | 〈〉 | 〈〉 | 〈如"已是百分比，formatPercent 不再 ×100；formatAchievementRate 会 ×100 误用〉 |
| 〈…〉 | | | | |

## 项目 feature flag 速查（兼容部署不踩坑）
> 调任何 bundle/聚合接口的新组件必须遵守同样的开关，否则在 legacy 部署红卡。

| Flag | 来源 | 当前用法 | 新组件必做 |
|------|------|---------|-----------|
| 〈VITE_ENABLE_BUNDLE_ROUTES〉 | 〈import.meta.env〉 | 〈Panel / Dashboard 通过 enabled: FLAG 短路〉 | 〈import + hook 的 enabled + render 短路〉 |
| 〈…〉 | | | |

## 业务公式 SSOT 路径（前端镜像协议）
> 若简报包含"当期目标"/"缺口"/"率"等需要前端复算的公式，标明后端 SSOT 路径，前端镜像必须：(a) 引用后端文件路径 (b) 单元测试锁口径 (c) 漂移防御注释。

| 公式 | 后端 SSOT | 单位/口径要点 |
|------|----------|-------------|
| 〈getPlanDenominator(timePeriod)〉 | 〈server/.../shared.ts:行号〉 | 〈年度值 ÷ 365/52/12/4/1 → 当期目标〉 |
| 〈…〉 | | |

## 要避免的默认（黑名单 — 逐条点名模型已知默认，见 context-spec §5）
- ❌渐变网格/噪点/颗粒/任意渐变背景　❌玻璃拟态/戏剧化大阴影/装饰几何/自定义光标　❌不对称·重叠·对角线布局　❌彩虹色阶（用单色顺序阶）　❌通用字体(Arial/Inter)做数字　❌把数据工具做成留白过度的营销页　❌注意力消耗型动画　❌〈项目专属…〉

## 红线（不要做）
- ❌ 删任何数据维度或交互  ❌ 用设计系统之外的色彩语言  ❌ 率值多于约定小数位 / 给纯计数列乱涂色  〈+ 项目特定红线〉

## 交付物
- 桌面宽屏（〈如 ≥1440px〉）高保真稿，导出 standalone HTML；如有余力补响应式。
- 标注关键状态（hover / 选中 / 下钻展开 / 联动）的视觉表达。
```
